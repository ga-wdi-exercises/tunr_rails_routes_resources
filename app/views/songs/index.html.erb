<h2>Songs <%= link_to "(+)", new_artist_song_path(@artist) %></h2>

<ul>
  <% @songs.each do |song| %>
    <li>
      <%= link_to "#{song.title} - #{song.artist.name}", artist_song_path(@artist, song) %>
    </li>
  <% end %>
</ul>
